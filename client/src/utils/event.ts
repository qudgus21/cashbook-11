// import { $ } from "./select"
// import { addClassSelector, removeClassSelector } from "./selectHandler"


// export const calendarResize = (e) => {
//     if (location.pathname !== '/calendar') return;

//     const calendarWrapper = $('.container-calendar').get()

//     if (matchMedia("screen and (min-width: 900px)").matches) {
//         if (calendarWrapper.classList.contains('calendar-min')) { 
//             removeClassSelector(calendarWrapper, 'calendar-min');
//         }

//     } else if (!calendarWrapper.classList.contains('calendar-min')) {
//         addClassSelector(calendarWrapper, 'calendar-min');

//     }
// }





// export const removeHandler = () => { 
//     window.removeEventListener('resize', calendarResize)
// }










